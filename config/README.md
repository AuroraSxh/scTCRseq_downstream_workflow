# Config

This folder stores analysis configuration files.

## Files

- `pbmc_10k_multi.yaml`: input paths, QC thresholds, clustering resolution, and clonotype plotting settings for the PBMC 10k workflow.

## Use

- Edit this file when you want to change thresholds or point the pipeline to a different dataset.
- After changing the config, rerun only the affected downstream steps.
